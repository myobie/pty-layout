node_modules/
pty.toml
.claude-session-id
# Build outputs — regenerated by `npm run build`, shipped in the npm tarball.
dist/
shim/tmux-main.js
shim/tmux-log-main.js
# Runtime-only files zsh writes to ZDOTDIR=shim/shell-init when we spawn it
shim/shell-init/.zsh_history
shim/shell-init/.zsh_sessions/
shim/shell-init/.zcompdump*
# npm pack tarballs
*.tgz
